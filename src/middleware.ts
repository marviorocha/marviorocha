// import { defineMiddleware } from 'astro/middleware';

// export const onRequest = defineMiddleware(({ request, redirect }) => {
//     const lang = request.headers.get("accept-language")?.startsWith("pt") ? "pt" : "en";
//     return redirect(`/${lang}/`);
// });
